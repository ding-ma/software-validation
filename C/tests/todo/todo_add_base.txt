@pytest.mark.monitor_test
def test_add_X_todo_t1():
    p = start_server()
    for i in range(X):
        create_to_do(str(i))
        assert_todos(str(i))
    shutdown_server(p)


@pytest.mark.monitor_test
def test_add_X_todo_t2(app):
    for i in range(X):
        create_to_do(str(i))
